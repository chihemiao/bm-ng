# AGENTS.md — 本仓库的 AI 工作规则

本文件对所有在本仓库工作的 AI agent 生效，优先级仅次于 `GOAL.md`。
冲突时以 `GOAL.md` 为准；本文件与用户单次指令冲突时，**先指出冲突，再等待确认**。

---

## 1. 绝对禁止

- **不得创建、读取或写入任何 mainnet 私钥、API key、助记词。** 遇到疑似凭据立即停止并报告。
- **不得提交 mainnet 订单，不得编写指向 mainnet endpoint 的下单路径。** 本 Goal 是 testnet-only。
- **不得放宽、注释掉、跳过或"临时禁用"任何 CI 门禁。** 门禁挡住了代码，说明代码要改，不是门禁要改。
- **不得新建版本化文件名**：`_v2` `_new` `_fixed` `_final` `_copy` `_old` `_backup` `_retry1` `_attempt1` 等。
- **不得新建 markdown 文档**来记录进度、总结、计划或调查结果。这些进对话或事件流，不进仓库。
- **不得为"以后可能需要"编写任何代码。** 没有当前失败测试驱动的代码一律不写。

## 2. 修复流程（唯一允许的顺序）

1. 写一个**失败的测试**复现问题
2. **修改现有文件**让它通过
3. 删掉过程中产生的所有临时脚本和调试文件
4. 确认 CI 全绿

**新建文件是例外，不是默认。** 需要新文件时，先用一句话说明"为什么这个逻辑不能放进现有的哪个文件"，等确认后再建。

## 3. 每次回复必须报告

```
运行闭包：xxxx / 8000 行   文件数：xx / 40
本次净增：+xx 行           新建文件：0
CI：全绿 / 失败项
```

数字对不上时不要继续写代码，先说明原因。

## 4. 不确定时的行为

**默认是停下来问，不是猜。** 这与 `GOAL.md` 的 fail-closed 原则一致。特别是：

- 交易所 API 语义不确定（资金费结算口径、精度规则、nonce 行为）→ 查官方文档，查不到就停下来说"这一条我不确定"
- 对账逻辑有多种合理解释 → 列出选项，不要选一个然后往下写
- 测试通过但你不确定它是否真的验证了目标行为 → 明说

**禁止用 mock 掩盖真实失败模式。** Gate 3 的故障注入必须注入真实的断线、超时、乱序，不能是 `mock.side_effect = TimeoutError`。

## 5. 反膨胀的具体表现

以下都是上一个项目的实际死因，识别到就停：

- "我先加一个 helper 文件" → 不行，找现有文件
- "保留旧实现以防万一" → 不行，git 保管历史
- "这个函数太复杂了，我拆成三个文件" → 拆成三个函数，同一个文件
- "为了记录这次尝试，我建一个 attempts/ 目录" → 尝试记录进事件流，不进源码
- "先写个脚手架，逻辑以后填" → 不写
- "这个检查太严了，我加个 skip" → 见 §1

## 6. 每个 PR

- 净增 ≤200 行（豁免每月 1 次，用完即止）
- 单一目的，能用一句话说清
- 不混合"修复 + 重构 + 新功能"
- commit message 说明改了什么行为，不是改了哪些文件

## 7. 关于 Goal 文档

`GOAL.md` ≤300 行且由 CI 检查。若你认为它需要修改：

- **直接改这个文件**，不新建版本
- 加一条规则，不加一个 Gate
- 加内容前先找出等量可删的旧内容
- 改动理由写进 PR 描述，不写进文档本身
